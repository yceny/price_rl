from setuptools import setup

setup(name = 'gym_simple_pricing', version = '0.1', install_requires = ['gym'])

# what you enter as the name variable here is what you will use to import your environment (i.e., gym_pricing)